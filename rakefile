require 'rubygems'
require 'bundler'
Bundler.setup

require 'spec'
require 'spec/rake/spectask'

Spec::Rake::SpecTask.new do |t|
  t.spec_files = FileList['spec/**/*.rb']
  t.spec_opts  = %w<-c>
end

task :default => :spec